/**
 * useOutsideInteraction — Outside 씬의 근접 감지 및 오브젝트 상호작용 훅.
 * 현재는 스텁(stub)이며, 씬에 인터랙티브 오브젝트가 추가될 때 로직을 확장합니다.
 */
export function useOutsideInteraction() {
  // TODO: 근접 감지, NPC 대화, 오브젝트 상호작용 로직 추가
}
